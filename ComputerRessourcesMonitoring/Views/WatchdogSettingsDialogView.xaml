<Window x:Class="ComputerRessourcesMonitoring.Views.WatchdogSettingsDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComputerRessourcesMonitoring.Views"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        mc:Ignorable="d"
        x:Name="DialogWindow"
        Title="Watchdog settings"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="WidthAndHeight"
        MinHeight="800"
        MinWidth="100"
        >
    <Window.Resources>
        <Style x:Key="{x:Type Button}"
               TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="ButtonBorder"
                                    BorderThickness="2" 
                                    CornerRadius="2" 
                                    Margin="0" 
                                    Padding="6"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="Black"
                                    Opacity="0.9">

                                    <TextBlock x:Name="ButtonContentTemplate"
                                               Text="{TemplateBinding Content}" 
                                               FontSize="16"
                                               FontFamily="Consola"
                                               FontWeight="DemiBold"
                                               Foreground="{TemplateBinding Foreground}"
                                               Background="{TemplateBinding Background}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="0"
                                               />

                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.4"/>
                                <Setter TargetName="ButtonContentTemplate" Property="Margin" Value="1"/>
                                <Setter TargetName="ButtonContentTemplate" Property="FontSize" Value="18"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type CheckBox}" x:Key="CheckBoxStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name ="checkboxImageParent" 
                                       CornerRadius="4"
                                       Background="#0f9900"
                                       Width="12"
                                       Height="12"
                                       Margin="0 0 5 0"
                                       >
                                <Ellipse x:Name="checkboxImageChild"
                                         Height="5"
                                         Width="5"
                                         Fill="Black"
                                         Opacity="0.8"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkboxImageChild" Property="Fill" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="checkboxImageChild" Property="Fill" Value="Transparent"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="checkboxImageParent" Property="Background" Value="#8ffc83"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="checkboxImageParent" Property="Background" Value="#8ffc83"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <Border BorderBrush="DarkGreen"
            BorderThickness="5"
            CornerRadius="30 30 30 30">
        <Border.Background>
            <SolidColorBrush Color="LightGreen" Opacity="0.8" />
        </Border.Background>

        <Grid Margin="30">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock  Background="Transparent"
                                FontWeight="Bold"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontFamily="Consola"
                                FontSize="20"
                                Foreground="DarkGreen"
                                TextDecorations="Underline"
                                Text ="Process under watch"
                                Margin="10 10 10 20"
                                />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <ContentControl>
                        <Image Source="Images/eye2.ico" Height="30"/>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseDown">
                                <i:InvokeCommandAction Command="{Binding DragWindowCommand}"
                                                       CommandParameter="{x:Reference Name=DialogWindow}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ContentControl>
                    <TextBox x:Name="target_process" 
                                             Text="{Binding WatchdogTargetName, UpdateSourceTrigger=PropertyChanged}" 
                                             Margin="10"
                                             MinWidth="200"
                                             BorderBrush="{x:Null}"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             FontWeight="Bold"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             FontFamily="Consola"
                                             FontSize="18"
                                             Foreground="DarkGreen"
                                             Padding="0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding ChangeWatchdogTargetCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Command="{Binding ChangeWatchdogTargetCommand}"
                            Background="#B58628"
                            Foreground="Black"
                            Content="CHANGE"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock      x:Name="ComputerResourcesTextBlock"
                                    Background="Transparent"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontFamily="Consola"
                                    FontSize="20"
                                    Foreground="DarkGreen"
                                    FontWeight="Bold"
                                    TextDecorations="Underline"
                                    Text ="CPU Resources Info"
                                    Margin="10 20 10 0"
                                    Width="300"
                                    />
                </StackPanel>
                <StackPanel Orientation="Vertical"
                                Width="{Binding ElementName=ComputerResourcesTextBlock, Path=ActualWidth}">
                        <ItemsControl HorizontalAlignment="Left"
                                         VerticalAlignment="Center"
                                         Background="Transparent"
                                         BorderBrush="Transparent"
                                         BorderThickness="0"
                                         FontFamily="Consola"
                                         FontSize="16"
                                         Foreground="DarkGreen"
                                         FontWeight="Bold"
                                         IsEnabled="False"
                                         ItemsSource="{Binding CpuUsageCollection, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Center"
                                                        Margin="0 10 0 0">
                                        <TextBlock Text="CPU: " Margin="0 0 5 0"/>
                                        <TextBlock Text="{Binding Cpu_Name}" Margin="0 0 5 0"/>
                                        <TextBlock Text="{Binding Cpu_Usage, StringFormat={}{0}%}" Margin="0 0 5 0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock
                            Background="Transparent"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="Consola"
                            FontSize="16"
                            Foreground="DarkGreen"
                            FontWeight="Bold"
                            Text ="{Binding CpuMake}"
                            TextWrapping="Wrap"
                            Margin="10 20 10 0"
                            Width="{Binding ElementName=ComputerResourcesTextBlock, Path=ActualWidth}"
                                />
                        <CheckBox 
                            Margin="0 20 0 0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="Consola"
                            FontSize="11"
                            Foreground="DarkGreen"
                            FontWeight="Bold"
                            Style="{StaticResource CheckBoxStyle}"
                            IsChecked="{Binding UsePerformanceCounterForCpuUsage}"
                            >
                        <CheckBox.Content>
                            <TextBlock Text="Use Performance Counter"
                                       VerticalAlignment="Center"
                                       Margin="0"/>
                        </CheckBox.Content>
                            <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.5" ScaleY="1.5" />
                            </CheckBox.LayoutTransform>
                        </CheckBox>



                </StackPanel>
                <TextBlock      x:Name="GpuResourcesTextBlock"
                                Background="Transparent"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontFamily="Consola"
                                FontSize="20"
                                Foreground="DarkGreen"
                                FontWeight="Bold"
                                TextDecorations="Underline"
                                Text ="GPU Resources Info"
                                Margin="10 20 10 0"
                                Width="{Binding ElementName=ComputerResourcesTextBlock, Path=ActualWidth}"
                                />
                <StackPanel Orientation="Vertical"
                                Width="{Binding ElementName=GpuResourcesTextBlock, Path=ActualWidth}">
                    <ItemsControl HorizontalAlignment="Left"
                                         VerticalAlignment="Center"
                                         BorderThickness="0"
                                         FontFamily="Consola"
                                         FontSize="16"
                                         FontWeight="Bold"
                                         IsEnabled="False"
                                         Background="Transparent"
                                         BorderBrush="Transparent"
                                         Foreground="DarkGreen"
                                         ItemsSource="{Binding GpuUsageCollection, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Margin="0 10 0 0">
                                    <TextBlock Text="{Binding Temperature, StringFormat={}GPU_Temp: {0}°C}" Margin="0 0 5 0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock
                            Background="Transparent"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            FontFamily="Consola"
                            FontSize="16"
                            Foreground="DarkGreen"
                            FontWeight="Bold"
                            Text ="{Binding GpuMake}"
                            TextWrapping="Wrap"
                            Margin="0 20 10 0"
                            Width="{Binding ElementName=ComputerResourcesTextBlock, Path=ActualWidth}"
                                />
                </StackPanel>

                <DockPanel Margin="20 80 20 20">
                    <StackPanel 
                    DockPanel.Dock="Bottom" 
                    Orientation="Horizontal" 
                    Margin="0" 
                    HorizontalAlignment="Center">
                        <Button 
                        IsDefault="True" 
                        Command="{Binding OkCommand}" 
                        Margin="0 0 20 0" 
                        Padding="0"
                        Content="OK"
                        Background="Green"/>

                        <Button 
                        IsCancel="True" 
                        Command="{Binding CancelCommand}"
                        Margin="20 0 0 0"
                        Background="#9e2020"
                        Content="CANCEL"/>
                    </StackPanel>
                </DockPanel>
            </StackPanel>

        </Grid>
    </Border>
</Window>
