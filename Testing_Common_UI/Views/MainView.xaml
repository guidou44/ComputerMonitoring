<Window x:Class="Testing_Common_UI.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Testing_Common_UI.Views"
        xmlns:ViewModels="clr-namespace:Testing_Common_UI.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Window.DataContext>
        <ViewModels:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
            <Button Width="200" Height="60" Background="Red" Content="Show error message" Command="{Binding TestErrorMessageCommand}" Margin="0 0 0 20"/>
            <Button Width="200" Height="60" Background="Red" Content="Show message" Command="{Binding TestMessageCommand}"/>
        </StackPanel>

    </Grid>
</Window>
